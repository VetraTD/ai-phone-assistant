
  LVX81 — blind rating scored against the pre-registered verdicts
  ================================================================

  RATING SPREAD
    british           39  #######################################
    american           1  #

    98% of takes carry one label. Two readings fit that and
    this data cannot separate them: the audio really was that uniform,
    or the rating did not discriminate. Weigh every verdict below
    against that, and note the rig did NOT reproduce the reported
    defect -- which is itself the most important thing here.

  PER VOICE
    Aoede      5 takes  all british   5 british
    Charon     5 takes  SCATTERED     4 british, 1 american
    Fenrir     5 takes  all british   5 british
    Kore       5 takes  all british   5 british
    Leda       5 takes  all british   5 british
    Orus       5 takes  all british   5 british
    Puck       5 takes  all british   5 british
    Zephyr     5 takes  all british   5 british

  VERDICTS, applied as written

  V1  is session-level drift the mechanism?
      Kore's 5 takes fall in 1 category.
      REFUTED, by the rule written before the run:
      "LVX81 is not a voice-selection problem. Close 'try the other prebuilt voices' as a non-fix and record that the drift is within-call, which no config lever reaches."

  V2  does any candidate beat Kore?
      No. The rule requires a candidate to be five-of-five british AND
      Kore not to be. Kore is. No candidate can win on this data.
      The incumbent stays, and no preference is written down as a decision.

  V3  does Live have a fix at all?
      Not confirmed. Voices consistent across all takes: Aoede, Fenrir, Kore, Leda, Orus, Puck, Zephyr

  ----------------------------------------------------------------
  What this round CANNOT say, from verdicts-voice.json:
    - Within-call drift. Every take is one short utterance from a fresh session; a session that wanders over four minutes is a different measurement.
    - Anything about the real prompt. These render with a minimal instruction, not the 24,000-character production one.
    - Whether a voice that is stable here stays stable under interruption, tool calls or a long call.

